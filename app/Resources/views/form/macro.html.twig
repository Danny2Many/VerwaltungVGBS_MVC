{% macro CollectionItem(formData, method, removetext) %}
    
    
        
        {% set name = formData.vars.full_name %}
    
    
    <div class="form-group" data-content="{{ name }}">
        {{ form_label(attribute(formData, method)) }}
    <div class="col-sm-3">
   {{ form_widget(attribute(formData, method)) }}
   
    </div>
   
       <div class="col-sm-7">
          
           <a class="btn-remove" data-related="{{ name }}"><button class="btn btn-danger" type="button"><span class="glyphicon glyphicon-remove"></span> {{ removetext }}</button></a>
           
       </div>
   
    </div>
{% endmacro %}


{#{% macro tfocusCollectionItem(formData) %}
    <div class="form-group">
    {{ form_label(formData.focus) }}
    <div class="col-sm-3">
   {{ form_widget(formData.focus) }}
   
    </div>
   
       <div class="col-sm-7">
          
           <button class="btn btn-danger" type="button" onclick="removeParent(this)"><span class="glyphicon glyphicon-remove"></span> Schwerpunkt löschen</button>
           
       </div>
   
    </div>
{% endmacro %}#}
